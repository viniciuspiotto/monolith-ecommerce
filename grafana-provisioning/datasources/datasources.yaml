apiVersion: 1

datasources:
  - name: Prometheus_Local
    type: prometheus
    url: http://prometheus_dev:9090
    isDefault: true
    access: proxy
    editable: true

  - name: Jaeger_Local
    type: jaeger
    url: http://jaeger_dev:16686
    access: proxy
    editable: true